import 'package:flutter/material.dart';

class MyHouseMaps extends StatelessWidget {
  const MyHouseMaps({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text('My House Maps'),
      ),
    );
  }
}
